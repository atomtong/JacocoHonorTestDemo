<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#222</item>
        <item name="colorPrimaryDark">#222</item>
        <item name="colorAccent">#000</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.List">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="toolbar_text_style">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>
    <style name="Splash" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/launcher_layer</item>
    </style>

    <style name="ThemeToolbar" parent="ThemeOverlay.AppCompat.Light">
        <!-- navigation icon color -->
        <item name="colorControlNormal">#000000</item>
        <!-- color of the menu overflow icon -->
        <!-- Used toUser color the overflow menu icon -->
        <item name="actionMenuTextColor">#000000</item>
    </style>

</resources>
